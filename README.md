# metro
METRO é um motor de extração, transferência e replicação de dados entre fontes estruturadas e não estruturadas e camadas de armazenamento baseadas em objetos, utilizando Parquet como formato de persistência.
